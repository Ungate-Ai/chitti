apiVersion: v1
kind: Service
metadata:
  name: chitti-service
spec:
  selector:
    app: chitti
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
    - name: metrics
      protocol: TCP
      port: 4000
      targetPort: 4000
  type: ClusterIP
